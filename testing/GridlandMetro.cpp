#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,m,k;
    cin >> n >> m >> k >> >> r >> c1 >> c2;
    int r[k],c1[k],c2[k];
    for (int i=0 ; i<k ; i++)cin >> r[i] >> c1[i] >> c2[i];

    return 0;
}
